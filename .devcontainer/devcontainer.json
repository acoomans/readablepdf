{
  "name": "readablepdf-dev",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.13-bookworm",
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends tesseract-ocr poppler-utils && sudo rm -rf /var/lib/apt/lists/*",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  }
}
